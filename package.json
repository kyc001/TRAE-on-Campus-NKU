{
  "name": "trae-on-campus",
  "version": "1.0.0",
  "scripts": {
    "dev": "vercel dev",
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "@vercel/node": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "typescript": "^5.2.2"
  }
}
